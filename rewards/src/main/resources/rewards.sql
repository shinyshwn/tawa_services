CREATE TABLE REWARDS (
                         rewardId INT AUTO_INCREMENT PRIMARY KEY,
                         USER_ID INT,
                         ORDER_ID INT,
                         POINTS INT,
                         TIME_STAMP TIMESTAMP
);
